scripted_gui = {

	UKR_SAM_state_view_gui = {

		context_type = selected_state_context
		parent_window_token = selected_state_view
		window_name = "UKR_SAM_state_view"

		visible = {
			is_owned_by = ROOT

			OR = {
				check_variable = {
					var = SAM_s300ps_count
					value = 0
					compare = greater_than
				}

				check_variable = {
					var = SAM_patriot_pac3_count
					value = 0
					compare = greater_than
				}
			}
		}

		triggers = {

			sam_s300ps_visible = {
				check_variable = {
					var = SAM_s300ps_count
					value = 0
					compare = greater_than
				}
			}

			sam_patriot_pac3_visible = {
				check_variable = {
					var = SAM_patriot_pac3_count
					value = 0
					compare = greater_than
				}
			}
		}
	}
}