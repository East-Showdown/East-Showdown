scripted_gui = {

	new_gui_topbar = {
    	window_name = "new_gui_topbar_window"
    	context_type = player_context
    	parent_window_name = countrypoliticsview

	    effects = {
	      	new_gui_button_open_click = {
	        	set_country_flag = open_new_gui
	      	}
	    }
	}

	new_gui_scripted_gui_window = {
    	window_name = "new_gui_scripted_gui_window"
    	context_type = player_context
    	# несовместимо с parent_window_name = countrypoliticsview
    	
    	visible = {
      		has_country_flag = open_new_gui
    	}

    	ai_enabled = {
      		always = no
    	}

    	effects = {
	      	new_gui_button_close_click = {
	        	clr_country_flag = open_new_gui
	      	}
	    }
  	}

}