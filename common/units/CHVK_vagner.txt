sub_units = {
    CHVK_vagner = {
        group = CHVK_vagner
        sprite =  unit # Refers to <name>_entity in a gfx/entities/.*.asset file - Controls the unit used on the map
        
        # Controls the map icon category used for the unit
        map_icon_category = armored

        priority = 1    # Controls which icon is used if multiple subunits of the same type exist
        ai_priority = 1

        active = yes       # Whether the unit is usable without explicit unlocking in technology         # Used to define subunit as cavalry 
        special_forces = yes    # Used to define subunit as special forces          # Used to define subunit as marine      # Used to define subunit as mountaineers # Used to define subunit as paratroopers.   # Used to check if the unit will affect the template's speed, support companies have it set to no.
        # Sets the speed of subunit via equipment. Used for motorized/mechanized
        
        # How the unit is grouped division-wise, i.e. appearing in the Support tab. Not used for air or naval units
        
        # Internal types
        
        # Categories the unit belongs to.
        
        # Equipment that is required for this unit. Used when active = no
        
        # Equipment needed to produce this unit. Unit won't be available until the nation has this equipment
        
        # Modifiers control the stats of the unit. More detail under the Stats section.
        manpower = 500
        max_strength = 20
        training_time = 10
        weight = 5
        default_morale = 0.3
        max_organisation = 20  
        combat_width = 0        
        supply_consumption = 0.05
        # Modifiers that occur only in specific terrain
    }
}