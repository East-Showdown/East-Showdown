types = {
	# удары по военной инфрасруктуре
	# rus_arms_industry_strike = {
	# 	days_to_prepare = 1 #2
	# 	days_re_enable = 90
	# 	category = critical_military_infrastructure_strikes
	# 	command_power = 0 # command power allocation cost
	# 	ai_will_do = {
	# 		base = 1
	# 	}
	# 	allowed = {
	# 		has_dlc = "Gotterdammerung"
	# 		original_tag = RUS
	# 	}
	# 	# Raid Type is visible
	# 	visible = {

	# 	}
	# 	# Specific Target is visible
	# 	show_target = {
	# 		NOT = { is_in_faction_with = FROM }
	# 		var:target_state = {
	# 			OR = {
	# 				arms_factory > 0
	# 				ammunition_plant > 0
	# 				ammunition_silo > 0
	# 			}
	# 		}
	# 	}
	# 	# Can prepare
	# 	available = {
	# 		has_war_with = FROM
	# 	}
	# 	target_type = {
	# 		building = {
	# 			type = arms_factory
	# 		}
	# 		# building = {
	# 		# 	type = ammunition_plant
	# 		# }
	# 		building = {
	# 			type = ammunition_silo
	# 		}
	# 	}
	# 	#target_icon = GFX_canal_icon
	# 	launch_sound = raid_launch_air

	# 	arrow = {
	# 	    type = air
	# 	}
    #     unit_model = {
    #         type = equipment
    #     }

	# 	unit_requirements = {
	# 		equipment = {
	# 			type = { strategic_bomber }
	# 			amount = { min = 1 max = 12 }
	# 		}
	# 	}

	# 	additional_equipment = {
	# 		rus_light_guided_missile_equipment = 8
	# 	}

	# 	starting_point = {
	# 	    types = { air_base }
	# 	}

	# 	success_factors = {
	# 		success = {
	# 			base = 0.40
	# 			experience = {
	# 				weight = 0.2
	# 				start_weight = -0.2
	# 				reference = 1.0
	# 			}
	# 			reliability = {
	# 				reference = 1
	# 				weight = 0.05
	# 				start_weight = -0.1
	# 			}
	# 			intel = {
	# 				weight = 0.3
	# 				reference = 100
	# 			}
	# 			anti_air = {
	# 				reference = 5
	# 				weight = -0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = -0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		disaster = {
	# 			base = 0.20
	# 			anti_air = {
	# 				reference = 5
	# 				weight = 0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = 0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		critical = {
	# 			base = 0.10
	# 		}
	# 	}
	# 	success_levels = {
	# 		failure = {
	# 			victim_effects = {
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8 #ивент прописать
	# 							# }
	# 						}
	# 					}
	# 				}
	# 			}
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunitionPlant = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunition_silo = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				if = {
	# 					limit = { check_variable = { ammunitionPlant = 0 } }
	# 					add_to_temp_variable = { ammunitionPlant = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						var:actor_country = { has_war_with = var:ROOT.victim_country }
	# 					}
	# 					multiply_temp_variable = { raid_planes_lost = 0.25 }
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "raid_failed_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		limited_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 					set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.4
	# 					max = 0.6
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = armsFactory }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { armsFactory_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionPlant_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionSilo_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = armsFactory_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = arms_factory
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_silo > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_limited_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 					set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.6
	# 					max = 0.8
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = armsFactory }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { armsFactory_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionPlant_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionSilo_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = armsFactory_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = arms_factory
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_silo > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				# set_temp_variable = { raid_planes_lost = 0 }
	# 				# set_temp_variable_to_random = {
	# 				# 	var = raid_planes_lost
	# 				# 	max = 3
	# 				# }
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		critical_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 					set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.8
	# 					max = 1
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = armsFactory }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { armsFactory_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionPlant_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionSilo_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = armsFactory_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = arms_factory
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_silo > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				# set_temp_variable = { raid_planes_lost = 0 }
	# 				# set_temp_variable_to_random = {
	# 				# 	var = raid_planes_lost
	# 				# 	max = 3
	# 				# }
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_critical_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 	}
	# }

	#удары по энергетике
	# rus_energy_infrastructure_strikes = {
	# 	days_to_prepare = 1 #2
	# 	days_re_enable = 90
	# 	category = critical_military_infrastructure_strikes
	# 	command_power = 0 # command power allocation cost
	# 	ai_will_do = {
	# 		base = 1
	# 	}
	# 	allowed = {
	# 		has_dlc = "Gotterdammerung"
	# 		original_tag = RUS
	# 	}
	# 	# Raid Type is visible
	# 	visible = {
	# 	}
	# 	# Specific Target is visible
	# 	show_target = {
	# 		NOT = { is_in_faction_with = FROM }
	# 		var:target_state = {
	# 			OR = {
	# 				electrical_substation_big > 0
	# 				electrical_substation > 0
	# 				thermal_power_plant > 0
	# 				solar_power_plant > 0
	# 				wind_power_plant > 0
	# 			}
	# 		}
	# 	}
	# 	# Can prepare
	# 	available = {
	# 		has_war_with = FROM
	# 	}
	# 	target_type = {
	# 		building = {
	# 			type = electrical_substation_big
	# 		}
	# 		building = {
	# 			type = electrical_substation
	# 		}
	# 		# building = {
	# 		# 	type = thermal_power_plant
	# 		# }
	# 		# building = {
	# 		# 	type = solar_power_plant
	# 		# }
	# 		# building = {
	# 		# 	type = wind_power_plant
	# 		# }
	# 	}
	# 	#target_icon = GFX_canal_icon
	# 	launch_sound = raid_launch_air

	# 	arrow = {
	# 	    type = air
	# 	}
    #     unit_model = {
    #         type = equipment
    #     }

	# 	unit_requirements = {
	# 		equipment = {
	# 			type = { strategic_bomber }
	# 			amount = { min = 1 max = 12  }
	# 		}
	# 	}

	# 	additional_equipment = {
	# 		rus_medium_guided_missile_equipment = 18
	# 	}

	# 	starting_point = {
	# 	    types = { air_base }
	# 	}

	# 	success_factors = {
	# 		success = {
	# 			base = 0.40
	# 			experience = {
	# 				weight = 0.2
	# 				start_weight = -0.2
	# 				reference = 1.0
	# 			}
	# 			reliability = {
	# 				reference = 1
	# 				weight = 0.05
	# 				start_weight = -0.1
	# 			}
	# 			intel = {
	# 				weight = 0.3
	# 				reference = 100
	# 			}
	# 			anti_air = {
	# 				reference = 5
	# 				weight = -0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = -0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		disaster = {
	# 			base = 0.20
	# 			anti_air = {
	# 				reference = 5
	# 				weight = 0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = 0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		critical = {
	# 			base = 0.10
	# 		}
	# 	}
	# 	success_levels = {
	# 		failure = {
	# 			victim_effects = {
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8 #ивент прописать
	# 							# }
	# 						}
	# 					}
	# 				}
	# 			}
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				if = {
	# 					limit = { check_variable = { el_sub = 0 } }
	# 					add_to_temp_variable = { el_sub = 0.1 }
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "raid_failed_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		limited_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 					set_temp_variable = { ROOT.el_sub_big_tgt = THIS.building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub_tgt = THIS.building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp_tgt = THIS.building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp_tgt = THIS.building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp_tgt = THIS.building_level@wind_power_plant }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.4
	# 					max = 0.6
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = therm_pp }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_big_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { therm_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { slolar_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { wind_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = therm_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_big_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_tgt }
	# 				add_to_temp_variable = { max_clamp = slolar_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = wind_pp_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { electrical_substation_big > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation_big
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { electrical_substation > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = thermal_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { solar_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = solar_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { wind_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_limited_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 					set_temp_variable = { ROOT.el_sub_big_tgt = THIS.building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub_tgt = THIS.building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp_tgt = THIS.building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp_tgt = THIS.building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp_tgt = THIS.building_level@wind_power_plant }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.6
	# 					max = 0.8
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = therm_pp }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_big_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { therm_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { slolar_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { wind_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = therm_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_big_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_tgt }
	# 				add_to_temp_variable = { max_clamp = slolar_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = wind_pp_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { electrical_substation_big > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation_big
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { electrical_substation > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = thermal_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { solar_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = solar_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { wind_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		critical_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 					set_temp_variable = { ROOT.el_sub_big_tgt = THIS.building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub_tgt = THIS.building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp_tgt = THIS.building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp_tgt = THIS.building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp_tgt = THIS.building_level@wind_power_plant }
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.8
	# 					max = 1
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = therm_pp }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_big_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { therm_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { slolar_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { wind_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = therm_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_big_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_tgt }
	# 				add_to_temp_variable = { max_clamp = slolar_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = wind_pp_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { electrical_substation_big > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation_big
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { electrical_substation > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = thermal_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { solar_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = solar_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { wind_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_critical_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 	}
	# }

	# удары балистикой по заводам
	# rus_ballistic_missile_arms_industry_strike = {
	# 	days_to_prepare = 1 #2
	# 	days_re_enable = 90
	# 	category = critical_military_infrastructure_strikes
	# 	command_power = 0 # command power allocation cost
	# 	ai_will_do = {
	# 		base = 1
	# 	}
	# 	allowed = {
	# 		has_dlc = "Gotterdammerung"
	# 		original_tag = RUS
	# 	}
	# 	# Raid Type is visible
	# 	visible = {

	# 	}
	# 	# Specific Target is visible
	# 	show_target = {
	# 		NOT = { is_in_faction_with = FROM }
	# 		var:target_state = {
	# 			OR = {
	# 				arms_factory > 0
	# 				ammunition_plant > 0
	# 				ammunition_silo > 0
	# 			}
	# 		}
	# 	}
	# 	# Can prepare
	# 	available = {
	# 		has_war_with = FROM
	# 	}
	# 	target_type = {
	# 		building = {
	# 			type = arms_factory
	# 		}
	# 		# building = {
	# 		# 	type = ammunition_plant
	# 		# }
	# 		building = {
	# 			type = ammunition_silo
	# 		}
	# 	}
	# 	#target_icon = GFX_canal_icon
	# 	launch_sound = raid_launch_air

	# 	arrow = {
	# 	    type = air
	# 	}
    #     unit_model = {
    #         type = equipment
    #     }

	# 	unit_requirements = {
	# 		equipment = {
	# 			type = { rus_ballistic_missile_equipment }
	# 			amount = { min = 50 }
	# 			#max = 12 }
	# 		}
	# 	}

	# 	additional_equipment = {
	# 		rus_ballistic_missile_equipment = 8
	# 	}

	# 	starting_point = {
	# 	    types = { rocket_site }
	# 	}

	# 	success_factors = {
	# 		success = {
	# 			base = 0.40
	# 			experience = {
	# 				weight = 0.2
	# 				start_weight = -0.2
	# 				reference = 1.0
	# 			}
	# 			reliability = {
	# 				reference = 1
	# 				weight = 0.05
	# 				start_weight = -0.1
	# 			}
	# 			intel = {
	# 				weight = 0.3
	# 				reference = 100
	# 			}
	# 			anti_air = {
	# 				reference = 5
	# 				weight = -0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = -0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		disaster = {
	# 			base = 0.20
	# 			anti_air = {
	# 				reference = 5
	# 				weight = 0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = 0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		critical = {
	# 			base = 0.10
	# 		}
	# 	}
	# 	success_levels = {
	# 		failure = {
	# 			victim_effects = {
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8 #ивент прописать
	# 							# }
	# 						}
	# 					}
	# 				}
	# 			}
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunitionPlant = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunition_silo = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				if = {
	# 					limit = { check_variable = { ammunitionPlant = 0 } }
	# 					add_to_temp_variable = { ammunitionPlant = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						var:actor_country = { has_war_with = var:ROOT.victim_country }
	# 					}
	# 					multiply_temp_variable = { raid_planes_lost = 0.25 }
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "raid_failed_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		limited_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 					set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.4
	# 					max = 0.6
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = armsFactory }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { armsFactory_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionPlant_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionSilo_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = armsFactory_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = arms_factory
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_silo > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_limited_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 					set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.6
	# 					max = 0.8
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = armsFactory }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { armsFactory_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionPlant_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionSilo_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = armsFactory_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = arms_factory
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_silo > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		critical_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 					set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.8
	# 					max = 1
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = armsFactory }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { armsFactory_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionPlant_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { ammunitionSilo_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = armsFactory_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
	# 				add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = arms_factory
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { ammunition_silo > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
	# 					set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
	# 					set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
	# 				}
	# 				add_to_temp_variable = { armsFactory = 0.5 }
	# 				multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 				multiply_temp_variable = { ammunitionSilo = armsFactory }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { ammunition_silo = 0 } }
	# 					}
	# 					multiply_temp_variable = { ammunitionPlant = armsFactory }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { ammunitionPlant = 0 } }
	# 						}
	# 						multiply_temp_variable = { ammunition_silo = armsFactory }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_critical_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 	}
	# }

	# удары шахедами по энергетике
	# rus_shahed_energy_infrastructure_strikes = {
	# 	days_to_prepare = 1 #2
	# 	days_re_enable = 90
	# 	category = critical_military_infrastructure_strikes
	# 	command_power = 0 # command power allocation cost
	# 	ai_will_do = {
	# 		base = 1
	# 	}
	# 	allowed = {
	# 		has_dlc = "Gotterdammerung"
	# 		original_tag = RUS
	# 	}
	# 	# Raid Type is visible
	# 	visible = {

	# 	}
	# 	# Specific Target is visible
	# 	show_target = {
	# 		NOT = { is_in_faction_with = FROM }
	# 		var:target_state = {
	# 			OR = {
	# 				electrical_substation_big > 0
	# 				electrical_substation > 0
	# 				thermal_power_plant > 0
	# 				solar_power_plant > 0
	# 				wind_power_plant > 0
	# 			}
	# 		}
	# 	}
	# 	# Can prepare
	# 	available = {
	# 		has_war_with = FROM
	# 	}
	# 	target_type = {
	# 		building = {
	# 			type = electrical_substation_big
	# 		}
	# 		building = {
	# 			type = electrical_substation
	# 		}
	# 		# building = {
	# 		# 	type = thermal_power_plant
	# 		# }
	# 		# building = {
	# 		# 	type = solar_power_plant
	# 		# }
	# 		# building = {
	# 		# 	type = wind_power_plant
	# 		# }
	# 	}
	# 	#target_icon = GFX_canal_icon
	# 	launch_sound = raid_launch_air

	# 	arrow = {
	# 	    type = air
	# 	}
    #     unit_model = {
    #         type = equipment
    #     }

	# 	unit_requirements = {
	# 		equipment = {
	# 			type = { rus_guided_uav_equipment }
	# 			amount = { min = 24 }
	# 		}
	# 	}

	# 	additional_equipment = {
	# 		rus_guided_uav_equipment = 24
	# 	}

	# 	starting_point = {
	# 	    types = { rocket_site }
	# 	}

	# 	success_factors = {
	# 		success = {
	# 			base = 0.40
	# 			experience = {
	# 				weight = 0.2
	# 				start_weight = -0.2
	# 				reference = 1.0
	# 			}
	# 			reliability = {
	# 				reference = 1
	# 				weight = 0.05
	# 				start_weight = -0.1
	# 			}
	# 			intel = {
	# 				weight = 0.3
	# 				reference = 100
	# 			}
	# 			anti_air = {
	# 				reference = 5
	# 				weight = -0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = -0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		disaster = {
	# 			base = 0.20
	# 			anti_air = {
	# 				reference = 5
	# 				weight = 0.40
	# 			}
	# 			air_defence = {
	# 				reference = 100
	# 				weight = 0.15
	# 				start_weight = 0.05
	# 			}
	# 		}
	# 		critical = {
	# 			base = 0.10
	# 		}
	# 	}
	# 	success_levels = {
	# 		failure = {
	# 			victim_effects = {
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8 #ивент прописать
	# 							# }
	# 						}
	# 					}
	# 				}
	# 			}
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				if = {
	# 					limit = { check_variable = { el_sub = 0 } }
	# 					add_to_temp_variable = { el_sub = 0.1 }
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "raid_failed_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		limited_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 					set_temp_variable = { ROOT.el_sub_big_tgt = THIS.building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub_tgt = THIS.building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp_tgt = THIS.building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp_tgt = THIS.building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp_tgt = THIS.building_level@wind_power_plant }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.4
	# 					max = 0.6
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = therm_pp }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_big_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { therm_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { slolar_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { wind_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = therm_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_big_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_tgt }
	# 				add_to_temp_variable = { max_clamp = slolar_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = wind_pp_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { electrical_substation_big > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation_big
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { electrical_substation > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = thermal_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { solar_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = solar_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { wind_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_limited_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 					set_temp_variable = { ROOT.el_sub_big_tgt = THIS.building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub_tgt = THIS.building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp_tgt = THIS.building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp_tgt = THIS.building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp_tgt = THIS.building_level@wind_power_plant }
	# 				}
	# 				if = {
	# 					limit = {
	# 						NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
	# 					}
	# 					hidden_effect = {
	# 						var:actor_country = {
	# 							set_variable = { THIS.target_country = var:ROOT.victim_country }
	# 							set_variable = { THIS.actor_country = var:ROOT.hostile_country }
	# 							# country_event = {
	# 							# 	id = MD_raid.8
	# 							# }
	# 						}
	# 					}
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.6
	# 					max = 0.8
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = therm_pp }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_big_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { therm_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { slolar_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { wind_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = therm_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_big_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_tgt }
	# 				add_to_temp_variable = { max_clamp = slolar_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = wind_pp_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { electrical_substation_big > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation_big
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { electrical_substation > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = thermal_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { solar_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = solar_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { wind_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 		critical_success = {
	# 		    victim_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 					set_temp_variable = { ROOT.el_sub_big_tgt = THIS.building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub_tgt = THIS.building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp_tgt = THIS.building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp_tgt = THIS.building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp_tgt = THIS.building_level@wind_power_plant }
	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 				set_temp_variable = { building_damage_by_missile = 0.25 }
	# 				set_temp_variable_to_random = {
	# 					var = missile_luck_mod
	# 					min = 0.8
	# 					max = 1
	# 				}
	# 				multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
	# 				divide_temp_variable = { building_damage_by_missile = therm_pp }
	# 				multiply_temp_variable = { building_damage_by_missile = 5 }
	# 				set_temp_variable = { damage_split = 0 }
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_big_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { el_sub_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { therm_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { slolar_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { wind_pp_tgt > 0 }
	# 					}
	# 					add_to_temp_variable = { damage_split = 0.1 }
	# 				}
	# 				if = {
	# 					limit = {
	# 						check_variable = { damage_split > 0.1 }
	# 					}
	# 					divide_temp_variable = { building_damage_by_missile = damage_split }
	# 				}
	# 				set_temp_variable = { max_clamp = therm_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_big_tgt }
	# 				add_to_temp_variable = { max_clamp = el_sub_tgt }
	# 				add_to_temp_variable = { max_clamp = slolar_pp_tgt }
	# 				add_to_temp_variable = { max_clamp = wind_pp_tgt }
	# 				clamp_temp_variable = {
	# 					var = building_damage_by_missile
	# 					min = 0
	# 					max = max_clamp
	# 				}
	# 				var:target_state = {
	# 					if = { limit = { electrical_substation_big > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation_big
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { electrical_substation > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = electrical_substation
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { arms_factory > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = thermal_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { solar_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = solar_power_plant
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 					if = { limit = { wind_power_plant > 0 }
	# 						meta_effect = {
	# 							text = {
	# 								damage_building = {
	# 									type = ammunition_silo
	# 									damage = [DAM] # levels of damage
	# 								}
	# 							}
	# 							DAM = "[?building_damage_by_missile]"
	# 						}
	# 					}
	# 				}
	# 		    }
	# 			actor_effects = {
	# 				var:target_state = {
	# 					set_temp_variable = { ROOT.el_sub_big = THIS.non_damaged_building_level@electrical_substation_big }
	# 					set_temp_variable = { ROOT.el_sub = THIS.non_damaged_building_level@electrical_substation }
	# 					set_temp_variable = { ROOT.therm_pp = THIS.non_damaged_building_level@thermal_power_plant }
	# 					set_temp_variable = { ROOT.slolar_pp = THIS.non_damaged_building_level@solar_power_plant }
	# 					set_temp_variable = { ROOT.wind_pp = THIS.non_damaged_building_level@wind_power_plant }

	# 				}
	# 				add_to_temp_variable = { therm_pp = 0.5 }
	# 				multiply_temp_variable = { el_sub_big = therm_pp }
	# 				multiply_temp_variable = { el_sub = therm_pp }
	# 				multiply_temp_variable = { slolar_pp = therm_pp }
	# 				multiply_temp_variable = { wind_pp = therm_pp }
	# 				if = {
	# 					limit = {
	# 						NOT = { check_variable = { el_sub_big = 0 } }
	# 					}
	# 					multiply_temp_variable = { el_sub_big = therm_pp }
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { el_sub = 0 } }
	# 						}
	# 						multiply_temp_variable = { el_sub = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { slolar_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { slolar_pp = therm_pp }
	# 					}
	# 					if = {
	# 						limit = {
	# 							NOT = { check_variable = { wind_pp = 0 } }
	# 						}
	# 						multiply_temp_variable = { wind_pp = therm_pp }
	# 					}
	# 				}
	# 			}
	# 			visual_effect = {
	# 				entity = "air_raids_2_critical_success_entity"
	# 				animation = "attack"
	# 			}
	# 		}
	# 	}
	# }

	#удары дронами украины по нпз рф
	ukr_strike_on_synthetic_refinery = {
		days_to_prepare = 1 #2
		days_re_enable = 90
		category = on_oil_refinery_strikes
		command_power = 0 # command power allocation cost
		ai_will_do = {
			base = 1
		}
		allowed = {
			has_dlc = "Gotterdammerung"
			original_tag = UKR
		}
		# Raid Type is visible
		visible = {
		}
		# Specific Target is visible
		show_target = {
			NOT = { is_in_faction_with = FROM }
			var:target_state = {
				OR = {
					synthetic_refinery > 0
				}
			}
		}
		# Can prepare
		available = {
			has_war_with = FROM
		}
		target_type = {
			building = {
				type = synthetic_refinery
			}
		}
		#target_icon = GFX_canal_icon
		launch_sound = raid_launch_air

		arrow = {
		    type = air
		}
        unit_model = {
            type = equipment
        }

		unit_requirements = {
			equipment = {
				type = { ukr_guided_uav_equipment }
				amount = { min = 24}
			}
		}

		additional_equipment = {
			ukr_guided_uav_equipment = 24
		}

		starting_point = {
		    types = { rocket_site }
		}

		success_factors = {
			success = {
				base = 0.40
				experience = {
					weight = 0.2
					start_weight = -0.2
					reference = 1.0
				}
				reliability = {
					reference = 1
					weight = 0.05
					start_weight = -0.1
				}
				intel = {
					weight = 0.3
					reference = 100
				}
				anti_air = {
					reference = 5
					weight = -0.40
				}
				air_defence = {
					reference = 100
					weight = -0.15
					start_weight = 0.05
				}
			}
			disaster = {
				base = 0.20
				anti_air = {
					reference = 5
					weight = 0.40
				}
				air_defence = {
					reference = 100
					weight = 0.15
					start_weight = 0.05
				}
			}
			critical = {
				base = 0.10
			}
		}
		success_levels = {
			failure = {
				victim_effects = {
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8 #ивент прописать
								# }
							}
						}
					}
				}
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.syntheticRefinery = THIS.non_damaged_building_level@synthetic_refinery }
					}
					add_to_temp_variable = { syntheticRefinery = 0.5 }
				}
				visual_effect = {
					entity = "raid_failed_entity"
					animation = "attack"
				}
			}
			limited_success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.syntheticRefinery = THIS.non_damaged_building_level@synthetic_refinery }

						set_temp_variable = { ROOT.syntheticRefinery_tgt = THIS.building_level@synthetic_refinery }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { syntheticRefinery = 0.5 }
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.4
						max = 0.6
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = syntheticRefinery }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { syntheticRefinery_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = syntheticRefinery_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { synthetic_refinery > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = synthetic_refinery
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.syntheticRefinery = THIS.non_damaged_building_level@synthetic_refinery }

					}
					add_to_temp_variable = { syntheticRefinery = 0.5 }
				}
				visual_effect = {
					entity = "air_raids_2_limited_success_entity"
					animation = "attack"
				}
			}
			success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.syntheticRefinery = THIS.non_damaged_building_level@synthetic_refinery }

						set_temp_variable = { ROOT.syntheticRefinery_tgt = THIS.building_level@synthetic_refinery }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { syntheticRefinery = 0.5 }
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.6
						max = 0.8
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = syntheticRefinery }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { syntheticRefinery_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = syntheticRefinery_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { synthetic_refinery > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = synthetic_refinery
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.syntheticRefinery = THIS.non_damaged_building_level@synthetic_refinery }

					}
					add_to_temp_variable = { syntheticRefinery = 0.5 }
				}
				visual_effect = {
					entity = "air_raids_2_success_entity"
					animation = "attack"
				}
			}
			critical_success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.syntheticRefinery = THIS.non_damaged_building_level@synthetic_refinery }

						set_temp_variable = { ROOT.syntheticRefinery_tgt = THIS.building_level@synthetic_refinery }
					}
					add_to_temp_variable = { syntheticRefinery = 0.5 }
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.8
						max = 1
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = syntheticRefinery }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { syntheticRefinery_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = syntheticRefinery_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { synthetic_refinery > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = synthetic_refinery
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.syntheticRefinery = THIS.non_damaged_building_level@synthetic_refinery }

					}
					add_to_temp_variable = { syntheticRefinery = 0.5 }
				}
				visual_effect = {
					entity = "air_raids_2_critical_success_entity"
					animation = "attack"
				}
			}
		}
	}

	#удары украины по военной промке рф
	ukr_arms_industry_strike = {
		days_to_prepare = 1 #2
		days_re_enable = 90
		category = critical_military_infrastructure_strikes
		command_power = 0 # command power allocation cost
		ai_will_do = {
			base = 1
		}
		allowed = {
			has_dlc = "Gotterdammerung"
			original_tag = UKR
		}
		# Raid Type is visible
		visible = {

		}
		# Specific Target is visible
		show_target = {
			NOT = { is_in_faction_with = FROM }
			var:target_state = {
				OR = {
					arms_factory > 0
					ammunition_plant > 0
					ammunition_silo > 0
				}
			}
		}
		# Can prepare
		available = {
			has_war_with = FROM
		}
		target_type = {
			building = {
				type = arms_factory
			}
			# building = {
			# 	type = ammunition_plant
			# }
			building = {
				type = ammunition_silo
			}
		}
		#target_icon = GFX_canal_icon
		launch_sound = raid_launch_air

		arrow = {
		    type = air
		}
        unit_model = {
            type = equipment
        }

		unit_requirements = {
			equipment = {
				type = { cas }
				amount = { min = 1 max = 12 }
			}
		}

		additional_equipment = {
			ukr_light_guided_missile_equipment = 16
			# OR = { #сука
			# 	nto_medium_guided_missile_equipment = 8
			# 	nto_light_guided_missile_equipment = 8
			# }
		}

		starting_point = {
		    types = { air_base }
		}

		success_factors = {
			success = {
				base = 0.40
				experience = {
					weight = 0.2
					start_weight = -0.2
					reference = 1.0
				}
				reliability = {
					reference = 1
					weight = 0.05
					start_weight = -0.1
				}
				intel = {
					weight = 0.3
					reference = 100
				}
				anti_air = {
					reference = 5
					weight = -0.40
				}
				air_defence = {
					reference = 100
					weight = -0.15
					start_weight = 0.05
				}
			}
			disaster = {
				base = 0.20
				anti_air = {
					reference = 5
					weight = 0.40
				}
				air_defence = {
					reference = 100
					weight = 0.15
					start_weight = 0.05
				}
			}
			critical = {
				base = 0.10
			}
		}
		success_levels = {
			failure = {
				victim_effects = {
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8 #ивент прописать
								# }
							}
						}
					}
				}
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunitionPlant = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunition_silo = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					if = {
						limit = { check_variable = { ammunitionPlant = 0 } }
						add_to_temp_variable = { ammunitionPlant = 0.1 }
					}
					if = {
						limit = {
							var:actor_country = { has_war_with = var:ROOT.victim_country }
						}
						multiply_temp_variable = { raid_planes_lost = 0.25 }
					}
				}
				visual_effect = {
					entity = "raid_failed_entity"
					animation = "attack"
				}
			}
			limited_success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
						set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.4
						max = 0.6
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = armsFactory }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { armsFactory_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionPlant_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionSilo_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = armsFactory_tgt }
					add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
					add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { arms_factory > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = arms_factory
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_plant > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_plant
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_silo > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_silo
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
				}
				visual_effect = {
					entity = "air_raids_2_limited_success_entity"
					animation = "attack"
				}
			}
			success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
						set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.6
						max = 0.8
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = armsFactory }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { armsFactory_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionPlant_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionSilo_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = armsFactory_tgt }
					add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
					add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { arms_factory > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = arms_factory
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_plant > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_plant
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_silo > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_silo
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
				}
				visual_effect = {
					entity = "air_raids_2_success_entity"
					animation = "attack"
				}
			}
			critical_success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
						set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.8
						max = 1
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = armsFactory }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { armsFactory_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionPlant_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionSilo_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = armsFactory_tgt }
					add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
					add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { arms_factory > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = arms_factory
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_plant > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_plant
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_silo > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_silo
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
				}
				visual_effect = {
					entity = "air_raids_2_critical_success_entity"
					animation = "attack"
				}
			}
		}
	}

	#удары украины западными ракетами по военке
	ukr_nto_arms_industry_strike = {
		days_to_prepare = 1 #2
		days_re_enable = 90
		category = critical_military_infrastructure_strikes
		command_power = 0 # command power allocation cost
		ai_will_do = {
			base = 1
		}
		allowed = {
			has_dlc = "Gotterdammerung"
			original_tag = UKR
		}
		# Raid Type is visible
		visible = {

		}
		# Specific Target is visible
		show_target = {
			NOT = { is_in_faction_with = FROM }
			var:target_state = {
				OR = {
					arms_factory > 0
					ammunition_plant > 0
					ammunition_silo > 0
				}
			}
		}
		# Can prepare
		available = {
			has_war_with = FROM
		}
		target_type = {
			building = {
				type = arms_factory
			}
			# building = {
			# 	type = ammunition_plant
			# }
			building = {
				type = ammunition_silo
			}
		}
		#target_icon = GFX_canal_icon
		launch_sound = raid_launch_air

		arrow = {
		    type = air
		}
        unit_model = {
            type = equipment
        }

		unit_requirements = {
			equipment = {
				type = { cas }
				amount = { min = 1 max = 12 }
			}
		}

		additional_equipment = {
			nto_medium_guided_missile_equipment = 16
		}

		starting_point = {
		    types = { air_base }
		}

		success_factors = {
			success = {
				base = 0.40
				experience = {
					weight = 0.2
					start_weight = -0.2
					reference = 1.0
				}
				reliability = {
					reference = 1
					weight = 0.05
					start_weight = -0.1
				}
				intel = {
					weight = 0.3
					reference = 100
				}
				anti_air = {
					reference = 5
					weight = -0.40
				}
				air_defence = {
					reference = 100
					weight = -0.15
					start_weight = 0.05
				}
			}
			disaster = {
				base = 0.20
				anti_air = {
					reference = 5
					weight = 0.40
				}
				air_defence = {
					reference = 100
					weight = 0.15
					start_weight = 0.05
				}
			}
			critical = {
				base = 0.10
			}
		}
		success_levels = {
			failure = {
				victim_effects = {
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8 #ивент прописать
								# }
							}
						}
					}
				}
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunitionPlant = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunition_silo = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					if = {
						limit = { check_variable = { ammunitionPlant = 0 } }
						add_to_temp_variable = { ammunitionPlant = 0.1 }
					}
					if = {
						limit = {
							var:actor_country = { has_war_with = var:ROOT.victim_country }
						}
						multiply_temp_variable = { raid_planes_lost = 0.25 }
					}
				}
				visual_effect = {
					entity = "raid_failed_entity"
					animation = "attack"
				}
			}
			limited_success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
						set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.4
						max = 0.6
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = armsFactory }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { armsFactory_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionPlant_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionSilo_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = armsFactory_tgt }
					add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
					add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { arms_factory > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = arms_factory
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_plant > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_plant
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_silo > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_silo
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
				}
				visual_effect = {
					entity = "air_raids_2_limited_success_entity"
					animation = "attack"
				}
			}
			success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
						set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.6
						max = 0.8
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = armsFactory }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { armsFactory_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionPlant_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionSilo_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = armsFactory_tgt }
					add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
					add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { arms_factory > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = arms_factory
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_plant > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_plant
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_silo > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_silo
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
				}
				visual_effect = {
					entity = "air_raids_2_success_entity"
					animation = "attack"
				}
			}
			critical_success = {
			    victim_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
						set_temp_variable = { ROOT.armsFactory_tgt = THIS.building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant_tgt = THIS.building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo_tgt = THIS.building_level@ammunition_silo }
					}
					if = {
						limit = {
							NOT = { var:actor_country = { has_war_with = var:ROOT.victim_country } }
						}
						hidden_effect = {
							var:actor_country = {
								set_variable = { THIS.target_country = var:ROOT.victim_country }
								set_variable = { THIS.actor_country = var:ROOT.hostile_country }
								# country_event = {
								# 	id = MD_raid.8
								# }
							}
						}
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
					set_temp_variable = { building_damage_by_missile = 0.25 }
					set_temp_variable_to_random = {
						var = missile_luck_mod
						min = 0.8
						max = 1
					}
					multiply_temp_variable = { building_damage_by_missile = missile_luck_mod }
					divide_temp_variable = { building_damage_by_missile = armsFactory }
					multiply_temp_variable = { building_damage_by_missile = 5 }
					set_temp_variable = { damage_split = 0 }
					if = {
						limit = {
							check_variable = { armsFactory_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionPlant_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { ammunitionSilo_tgt > 0 }
						}
						add_to_temp_variable = { damage_split = 0.1 }
					}
					if = {
						limit = {
							check_variable = { damage_split > 0.1 }
						}
						divide_temp_variable = { building_damage_by_missile = damage_split }
					}
					set_temp_variable = { max_clamp = armsFactory_tgt }
					add_to_temp_variable = { max_clamp = ammunitionPlant_tgt }
					add_to_temp_variable = { max_clamp = ammunitionSilo_tgt }
					clamp_temp_variable = {
						var = building_damage_by_missile
						min = 0
						max = max_clamp
					}
					var:target_state = {
						if = { limit = { arms_factory > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = arms_factory
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_plant > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_plant
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
						if = { limit = { ammunition_silo > 0 }
							meta_effect = {
								text = {
									damage_building = {
										type = ammunition_silo
										damage = [DAM] # levels of damage
									}
								}
								DAM = "[?building_damage_by_missile]"
							}
						}
					}
			    }
				actor_effects = {
					var:target_state = {
						set_temp_variable = { ROOT.armsFactory = THIS.non_damaged_building_level@arms_factory }
						set_temp_variable = { ROOT.ammunitionPlant = THIS.non_damaged_building_level@ammunition_plant }
						set_temp_variable = { ROOT.ammunitionSilo = THIS.non_damaged_building_level@ammunition_silo }
					}
					add_to_temp_variable = { armsFactory = 0.5 }
					multiply_temp_variable = { ammunitionPlant = armsFactory }
					multiply_temp_variable = { ammunitionSilo = armsFactory }
					if = {
						limit = {
							NOT = { check_variable = { ammunition_silo = 0 } }
						}
						multiply_temp_variable = { ammunitionPlant = armsFactory }
						if = {
							limit = {
								NOT = { check_variable = { ammunitionPlant = 0 } }
							}
							multiply_temp_variable = { ammunition_silo = armsFactory }
						}
					}
				}
				visual_effect = {
					entity = "air_raids_2_critical_success_entity"
					animation = "attack"
				}
			}
		}
	}

}