add_namespace = smo
add_namespace = smo_hidden

#Starting the SMO

country_event = {
    id = smo.1
    title = smo.1.t
    desc = smo.1.desc
    is_triggered_only = yes
    fire_only_once = yes
    option = {
        declare_war_on = {
            target = UKR
            type = annex_everything
                    }
    }
}

add_namespace = smo_news
news_event = {
    id = smo_news.1
    fire_only_once = yes
    title = {
        text = smo_news.1.t
		}
		is_triggered_only = yes
        trigger = {
            always = no
        }
    }

#Guarantee the Donbass republics

country_event = {
    id = smo.2
    title = smo.2.t
    desc = smo.2.desc
    is_triggered_only = yes
    fire_only_once = yes
    option = {
        add_to_faction = LNR
        add_to_faction = DNR
    }
}
